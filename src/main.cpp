#include <iostream>
#include <vector>

#include "surface.h"

int main() {
  NurbSurf sf(4);

  for (int i = 0; i < 1000; i++) {
    for (int j = 0; j < 1000; j++) {
      float u = (float) i / 999.0f;
      float v = (float) j / 999.0f;
      
    }
  }

  return 0;
}

